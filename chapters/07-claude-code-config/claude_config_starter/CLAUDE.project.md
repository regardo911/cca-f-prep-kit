<!-- Copy to ./CLAUDE.md at your project root. Project scope: ships with the repo. -->

# Project Rules
- Always run the test suite before suggesting a commit.
- All Python code must use the standard library's logging module, not print().
- New MCP servers must register a tool description that names the data source explicitly.
